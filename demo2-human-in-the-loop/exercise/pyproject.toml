[project]
name = "ai-agents-webinar-hitl"
version = "0.1"
description = "OpenAI Agents SDK + Temporal demo with human-in-the-loop + F1 MCP"
authors = [{ name = "Temporal Technologies Inc", email = "sdk@temporal.io" }]
requires-python = ">=3.10"
readme = "README.md"
license = "MIT"
dependencies = [
    "temporalio[openai-agents,opentelemetry]>=1.17.0,<2",
    "httpx>=0.27",
]
