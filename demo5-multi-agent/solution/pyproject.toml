[project]
name = "ai-agents-webinar-multi-agent"
version = "0.1"
description = "Multi-agent orchestration with Temporal child workflows + Nexus"
authors = [{ name = "Temporal Technologies Inc", email = "sdk@temporal.io" }]
requires-python = ">=3.10"
readme = "README.md"
license = "MIT"
dependencies = [
    "temporalio[openai-agents,opentelemetry]>=1.17.0,<2",
    "httpx>=0.27",
]
